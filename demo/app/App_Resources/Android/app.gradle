// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "com.xplorebits.demo.mapbox"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
