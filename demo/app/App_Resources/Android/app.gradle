// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "com.saikksub.nsvue.maps.demo"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
